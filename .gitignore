# Environment and secrets
.env
.env.*
!.env.example

# Logs
*.log
auth/debug.log
games/rawg_debug.log
igdb_debug.log
steam_debug.log
rawg_debug.log

# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Keep uploaded files out of git except existing profile images
# auth/uploads/ is intentionally not ignored so existing images deploy to Render
# Note: new uploads on Render will not persist across redeploys (no persistent disk)

# SCSS source files
scss/

# Dev notes
TESTING_CHECKLIST.md

# Dependency/vendor folders (uncomment if you add these later)
# vendor/
# node_modules/
